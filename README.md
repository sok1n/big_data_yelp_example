## virtual environment

Create the virtual environment:

python -m venv venv

Activate (windows)

venv\Scripts\activate

Activate(macOS/Linux)

source venv/bin/activate

Install dependeniced:

pip install -r requirements.txt